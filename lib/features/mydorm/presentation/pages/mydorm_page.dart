import 'package:flutter/material.dart';

class MyDormPage extends StatelessWidget {
  const MyDormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('My Dorm Page (Dummy)'));
  }
}
